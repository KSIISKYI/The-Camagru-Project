<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Recover</title>
	<style type="text/css">
		body {
    		font-family: 'trebuchet ms';
		}

		.page {
		    background-color: #F5FFFF;
			padding: 20px;
			width: 70%;
		    margin: 0 auto;
		}
		button {
		  background-color: #3485ae;
		  color: white;
		  padding: 14px 20px;
		  margin: 8px 0;
		  border: none;
		  cursor: pointer;
		  border-radius: 5px;
		}

		/* Add a hover effect for buttons */
		button:hover {
		  opacity: 0.8;
		}
	</style>
</head>
<body>
	<div class="page">
		<h1>Recovery Password</h1>
		<p>You are receiving this email because we received a password recovery request for your account.</p>
		<form action="{{ route({'name':'recovery_form'}) }}" method="POST">
			<input type="hidden" value="{{ recovery_token }}" name="token">
			<button type="submit">Recovery Password</button>
		</form>
		<p>If you did request a password recovery, no further action is request.</p>
		<h3>Camagru Team</h3>
	</div>
</body>
</html>